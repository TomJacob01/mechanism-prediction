# Repo map

Auto-generated by `python scripts/gen_repo_map.py`. **Do not edit by hand** — your changes will be overwritten. Regenerate after adding or removing modules.

Each entry shows: module path, module docstring (first line), and public classes/functions with one-line summaries.

## Library

### [src/mech_uspto/__init__.py](../src/mech_uspto/__init__.py)

_mech_uspto: dual-mode reaction prediction on mech-USPTO-31k._

### [src/mech_uspto/chemistry/__init__.py](../src/mech_uspto/chemistry/__init__.py)

_Chemistry primitives: applying Δ-matrix predictions back to RDKit molecules._

### [src/mech_uspto/chemistry/apply_delta.py](../src/mech_uspto/chemistry/apply_delta.py)

_Apply a Δ bond-order matrix to an RDKit ``Mol`` to obtain a new molecule._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class ApplyDeltaError` | Structured failure from :func:`apply_delta`. |
| func | `apply_delta(mol, delta, *, charge_delta, apply_charge_heuristic)` | Return a new ``Mol`` with ``delta`` applied to ``mol``'s bond orders. |

### [src/mech_uspto/constants.py](../src/mech_uspto/constants.py)

_Chemistry constants used by featurization._

### [src/mech_uspto/data/__init__.py](../src/mech_uspto/data/__init__.py)

_Data subpackage: parsing, featurization, dataset, loaders._

### [src/mech_uspto/data/arrow_parser.py](../src/mech_uspto/data/arrow_parser.py)

_Parse PMechDB-style curly arrows into elementary mechanistic steps._

| Kind | Symbol | Summary |
|---|---|---|
| func | `arrow_bond_changes(arrow, map_to_idx)` | Return ``[(i, j, ±1), ...]`` heavy-atom bond-order changes for one arrow. |
| func | `arrow_charge_changes(arrow, map_to_idx)` | Return ``[(heavy_idx, Δfc), ...]`` formal-charge changes for one arrow. |
| func | `group_arrows_into_steps(arrows, *, max_step_size)` | Group arrows into elementary steps via electron-flow chaining. |
| class | `class ElementaryStep` | One elementary mechanistic step. |
| func | `parse_arrows(label_str)` | Parse the ``mechanistic_label`` literal into a list of ``(src, tgt)``. |
| func | `parse_steps(label_str, map_to_idx)` | End-to-end: literal → arrows → chain-rule step groups → per-step deltas. |

### [src/mech_uspto/data/cache_schema.py](../src/mech_uspto/data/cache_schema.py)

_Parquet schemas + I/O helpers for the canonical training cache._

| Kind | Symbol | Summary |
|---|---|---|
| func | `split_hash(rxn_id)` | Deterministic 32-bit hash for train/val/test bucketing. |
| func | `open_writer(path, schema)` | Open a Zstd-compressed Parquet writer at ``path``. |
| func | `write_batch(writer, rows, schema)` | Append ``rows`` to ``writer`` as a single record batch. |

### [src/mech_uspto/data/dataset.py](../src/mech_uspto/data/dataset.py)

_Dual-mode dataset over mech-USPTO-31k reactions._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class MechUSPTODataset` | Unified dataset supporting both stepwise and end-to-end task modes. |

### [src/mech_uspto/data/featurization.py](../src/mech_uspto/data/featurization.py)

_Graph featurization for mapped SMILES._

| Kind | Symbol | Summary |
|---|---|---|
| func | `one_hot_encode(value, allowed_choices)` | One-hot encode ``value`` against ``allowed_choices``. |
| func | `align_atoms(mol)` | Reorder atoms so mapped atoms come first (sorted by map number). |
| func | `featurize_nodes(aligned_mol)` | Build the (N, 25) node-feature tensor. |
| func | `featurize_edges(aligned_mol)` | Build (edge_index, edge_attr) for the molecule. |
| func | `process_mapped_smiles(mapped_smiles, add_hs)` | Parse a mapped SMILES into an RDKit ``Mol`` and a PyG ``Data`` object. |

### [src/mech_uspto/data/loaders.py](../src/mech_uspto/data/loaders.py)

_Collation function and high-level dataloader factory._

| Kind | Symbol | Summary |
|---|---|---|
| func | `seed_everything(seed, deterministic)` | Seed every RNG that affects training, for run-to-run reproducibility. |
| func | `collate_fn_with_spectators(data_list)` | Collate a list of ``Data`` items, padding Δ matrices and spectator masks. |
| func | `create_dataloaders(csv_path, task_mode, batch_size, train_val_test_split, num_workers, seed, compute_spectators, cache_dir, use_cache)` | Parse ``csv_path``, build one cached dataset, return train/val/test DataLoaders. |

### [src/mech_uspto/data/parquet_dataset.py](../src/mech_uspto/data/parquet_dataset.py)

_PyG Dataset over the canonical Parquet cache._

| Kind | Symbol | Summary |
|---|---|---|
| func | `split_of(h)` | Map a ``split_hash`` value to one of ``train`` / ``val`` / ``test``. |
| class | `class ParquetMechDataset` | PyG ``Dataset`` backed by the parquet cache. |

### [src/mech_uspto/data/parser.py](../src/mech_uspto/data/parser.py)

_Parser for mech-USPTO-31k CSV files (figshare format)._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class MechUSPTOParser` | Parser for the mech-USPTO-31k CSV (figshare release). |

### [src/mech_uspto/data/schema.py](../src/mech_uspto/data/schema.py)

_Dataclasses representing a mech-USPTO-31k reaction._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class ReactionStep` | A single elementary step in a multi-step reaction. |
| class | `class MultiStepReaction` | A complete multi-step reaction from mech-USPTO-31k. |

### [src/mech_uspto/data/spectators.py](../src/mech_uspto/data/spectators.py)

_Identify spectator atoms (those whose bonds do not change)._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class SpectatorDetector` | Identify and summarise spectator atoms in USPTO reactions. |

### [src/mech_uspto/data/transformations.py](../src/mech_uspto/data/transformations.py)

_Compute Δ (delta) bond-order matrices for stepwise / end-to-end modes._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class DeltaMatrixGenerator` | Build delta matrices from molecule pairs or mechanism arrow strings. |

### [src/mech_uspto/losses/__init__.py](../src/mech_uspto/losses/__init__.py)

_Loss subpackage._

### [src/mech_uspto/losses/focal.py](../src/mech_uspto/losses/focal.py)

_Masked focal loss with spectator-atom downweighting._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class MaskedFocalLossWithSpectators` | Focal loss masked to valid positions, with optional spectator downweighting. |

### [src/mech_uspto/models/__init__.py](../src/mech_uspto/models/__init__.py)

_Model subpackage._

### [src/mech_uspto/models/heads.py](../src/mech_uspto/models/heads.py)

_Classification heads for delta-matrix prediction._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class DeltaMLP` | Pairwise MLP head producing per-pair Δ logits. |

### [src/mech_uspto/models/transformer.py](../src/mech_uspto/models/transformer.py)

_Reaction transformer encoder + delta head (adapted from PMechDB POC)._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class ReactionTransformer` | Graph-transformer encoder paired with a modular ``DeltaMLP`` head. |

### [src/mech_uspto/training/__init__.py](../src/mech_uspto/training/__init__.py)

_Training subpackage._

### [src/mech_uspto/training/config.py](../src/mech_uspto/training/config.py)

_Training configuration for the dual-mode ablation study._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class Config` | Mode-aware training configuration. |

### [src/mech_uspto/training/engine.py](../src/mech_uspto/training/engine.py)

_Training / evaluation engine for the ablation study._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class TrainingEngine` | Train + validate + checkpoint a ``ReactionTransformer``. |

### [src/mech_uspto/training/metrics.py](../src/mech_uspto/training/metrics.py)

_Metrics for delta-matrix prediction._

| Kind | Symbol | Summary |
|---|---|---|
| func | `binary_pr_auc(scores, targets)` | Average-precision (area under the precision-recall curve) for a binary task. |
| class | `class MetricsComputer` | Compute precision / recall / F1 / top-K for delta predictions. |
| func | `pooled_pr_auc(scores_list, targets_list, probs_list, class_targets_list, num_classes)` | Pool batch-level buffers and compute (overall, per-class) PR-AUC once. |

### [src/mech_uspto/training/tracking.py](../src/mech_uspto/training/tracking.py)

_Tracker abstraction so engine.train() doesn't import W&B / TensorBoard directly._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class Tracker` | Base class. No-op implementation — also used as the default. |
| class | `class WandbTracker` | Weights & Biases backend. Requires ``pip install wandb``. |
| func | `make_tracker(backend, **kwargs)` | Factory. ``backend`` is one of ``{"none", "wandb"}``. |

## Scripts

### [scripts/build_parquet_cache.py](../scripts/build_parquet_cache.py)

_Build the canonical Parquet cache from the verified rollout._

| Kind | Symbol | Summary |
|---|---|---|
| func | `rollout_clean(rxn_id, r_smi, p_smi, label_str, mechanistic_class, data_source)` | Run a single reaction's rollout. Return parquet rows iff fully clean. |
| func | `main()` |  |

### [scripts/cache_audit.py](../scripts/cache_audit.py)

_T1.4 + T2.{1,2,3} — Single-pass parquet-cache audit._

| Kind | Symbol | Summary |
|---|---|---|
| func | `main()` |  |

### [scripts/charge_diagnostic.py](../scripts/charge_diagnostic.py)

_Phase-0 diagnostic for the diagonal-Δ / charge encoding plan._

| Kind | Symbol | Summary |
|---|---|---|
| func | `main()` |  |

### [scripts/evaluate.py](../scripts/evaluate.py)

_Evaluate a trained checkpoint on the held-out test split._

| Kind | Symbol | Summary |
|---|---|---|
| func | `load_checkpoint(path, device)` | Load a training checkpoint; tolerate both new-style (with config) and old-style. |
| func | `build_model_from_config(config, device)` | Mirror the architecture wiring done by TrainingEngine.__init__. |
| func | `evaluate_split(model, loader, device, task_mode, num_classes)` | Run the model on ``loader`` and return aggregate metrics + confusion matrix. |
| func | `write_confusion_csv(confusion, path, shift)` | Write the confusion matrix as CSV with Δ-labeled rows/cols. |
| func | `parse_args()` |  |
| func | `main()` |  |

### [scripts/gen_repo_map.py](../scripts/gen_repo_map.py)

_Generate ``docs/repo-map.md`` — a per-module symbol index for AI agents._

| Kind | Symbol | Summary |
|---|---|---|
| class | `class SymbolRow` |  |
| class | `class ModuleEntry` |  |
| func | `main()` |  |

### [scripts/plot_history.py](../scripts/plot_history.py)

_Plot training history (loss + metrics) from a results JSON._

| Kind | Symbol | Summary |
|---|---|---|
| func | `load_history(results_path)` | Load and validate the history block from a results JSON. |
| func | `plot_history(results, output_path)` | Render a 4-panel figure: loss, F1/P/R, PR-AUC, rxn-prediction counts. |
| func | `parse_args()` |  |
| func | `main()` |  |

### [scripts/research/bucket_diverged.py](../scripts/research/bucket_diverged.py)

| Kind | Symbol | Summary |
|---|---|---|
| func | `classify(pred, true)` |  |

### [scripts/sanity_check.py](../scripts/sanity_check.py)

_Pre-flight sanity checks for a mech-USPTO-31k CSV data drop._

| Kind | Symbol | Summary |
|---|---|---|
| func | `ok(msg)` |  |
| func | `warn(msg)` |  |
| func | `fail(msg)` |  |
| func | `check_environment()` |  |
| func | `check_csv_layout(csv_path)` |  |
| func | `check_parsing(csv_path)` |  |
| func | `check_featurization(reactions, sample_size)` |  |
| func | `check_delta_stats(reactions, sample_size)` |  |
| func | `check_spectator_ratio(reactions, sample_size)` |  |
| func | `check_mini_training_step(reactions, batch_size)` |  |
| func | `main()` |  |

### [scripts/train.py](../scripts/train.py)

_CLI entrypoint for ablation-study training._

| Kind | Symbol | Summary |
|---|---|---|
| func | `parse_args()` |  |
| func | `main()` |  |

### [scripts/verify_apply_delta_e2e.py](../scripts/verify_apply_delta_e2e.py)

_End-to-end verification of :func:`apply_delta`._

| Kind | Symbol | Summary |
|---|---|---|
| func | `main()` |  |

### [scripts/verify_apply_delta_sequential.py](../scripts/verify_apply_delta_sequential.py)

_Sequential apply_delta verifier._

| Kind | Symbol | Summary |
|---|---|---|
| func | `main()` |  |

### [scripts/verify_arrow_parser.py](../scripts/verify_arrow_parser.py)

_Verify the curly-arrow parser on real USPTO-31k data._

| Kind | Symbol | Summary |
|---|---|---|
| func | `analyse_reaction(rxn_id, r_smi, p_smi, label_str)` | Return per-reaction result dict with rule-check + sequential-test outcomes. |
| func | `main()` |  |

### [scripts/verify_cache_rollout.py](../scripts/verify_cache_rollout.py)

_T1.2 — Sequential apply_delta over cached steps._

| Kind | Symbol | Summary |
|---|---|---|
| func | `main()` |  |

## Tests

### [tests/__init__.py](../tests/__init__.py)

### [tests/conftest.py](../tests/conftest.py)

_Shared pytest fixtures._

| Kind | Symbol | Summary |
|---|---|---|
| func | `sample_csv_path()` | Path to the bundled real-data CSV fixture (2 rows from figshare). |
| func | `sample_reactions()` | All reactions parsed from the sample CSV fixture. |
| func | `mock_reaction(sample_reactions)` | First reaction from the sample CSV (used for downstream tests). |
| func | `stepwise_dataset(mock_reaction)` |  |
| func | `end_to_end_dataset(mock_reaction)` |  |

### [tests/test_apply_delta.py](../tests/test_apply_delta.py)

_Unit tests for :func:`mech_uspto.chemistry.apply_delta.apply_delta`._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_break_bond_removes_bond()` |  |
| func | `test_form_bond_adds_bond()` |  |
| func | `test_change_order_single_to_double()` |  |
| func | `test_invalid_negative_order_raises()` |  |
| func | `test_invalid_quadruple_order_raises()` |  |
| func | `test_shape_mismatch_raises()` |  |
| func | `test_diagonal_is_ignored()` |  |
| func | `test_preserves_atom_maps()` |  |
| func | `test_roundtrip_via_delta_from_reactants_products()` | Δ from R→P, applied to R, should yield a Mol matching P. |
| func | `test_chirality_preserved_on_untouched_atom()` | An atom whose bonds are not in the Δ keeps its R/S tag. |
| func | `test_chirality_preserved_when_neighbor_elements_unchanged()` | Mitsunobu-like: touched atom keeps @ tag when neighbour element multiset |
| func | `test_chirality_blanked_when_neighbor_elements_change()` | Touched atom with neighbour element multiset CHANGED → UNSPECIFIED. |
| func | `test_bond_stereo_blanked_when_double_bond_touched()` | E/Z on a double bond touched by Δ must be blanked. |
| func | `test_explicit_charge_delta_overrides_heuristic()` | When charge_delta is provided, it sets the charges regardless of |
| func | `test_charge_heuristic_applies_when_no_explicit_delta()` | With apply_charge_heuristic=True and no charge_delta, the row-sum |
| func | `test_two_step_delta_preserves_atom_maps_and_indices()` | The sequential model relies on applying multiple Δ steps in sequence, |
| func | `test_two_step_delta_chirality_propagation()` | Chirality on an atom that's untouched across both steps must survive. |
| func | `test_new_stereocentre_emerges_as_unspecified()` | sp²→sp³ where R had no chirality must NOT invent a stereo tag. |
| func | `test_charge_delta_with_heuristic_disabled()` | Explicit charge_delta works even when apply_charge_heuristic=False. |
| func | `test_zero_delta_is_noop()` | A Δ of all zeros should return an identical molecule (atom maps, |

### [tests/test_apply_delta_e2e_slice.py](../tests/test_apply_delta_e2e_slice.py)

_Regression slice for apply_delta on real USPTO reactions._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_e2e_slice_recoverable_pass_rate()` |  |

### [tests/test_arrow_parser.py](../tests/test_arrow_parser.py)

_Smoke tests for the chain-rule arrow grouper._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_chain_rule_returns_all_arrow_indices()` | Every arrow must land in exactly one group, in order. |
| func | `test_chain_rule_groups_acyl_substitution()` | Acyl-Cl + NH3: chain rule fuses the 4 arrows into 1 concerted step. |
| func | `test_chain_rule_runs_on_sn2()` | Smoke: chain rule completes on a 2-arrow SN2 without crashing. |

### [tests/test_collation.py](../tests/test_collation.py)

_Tests for the collation function._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_collation_single_item(stepwise_dataset)` |  |
| func | `test_collation_filters_none_inputs(stepwise_dataset)` |  |
| func | `test_collation_all_none_returns_none()` |  |
| func | `test_create_dataloaders_smoke(sample_csv_path, tmp_path)` | Regression: the Subset-based refactor must produce working dataloaders. |
| func | `test_collation_does_not_mutate_input_data(stepwise_dataset)` | Regression: collate must NOT delete fields from the original Data objects. |

### [tests/test_dataset.py](../tests/test_dataset.py)

_Tests for ``MechUSPTODataset``._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_stepwise_dataset_builds(stepwise_dataset)` |  |
| func | `test_stepwise_delta_in_unit_range(stepwise_dataset)` |  |
| func | `test_end_to_end_dataset_builds(end_to_end_dataset)` |  |
| func | `test_end_to_end_delta_in_extended_range(end_to_end_dataset)` |  |
| func | `test_invalid_task_mode_raises(mock_reaction)` |  |
| func | `test_empty_steps_list_yields_empty_dataset(mock_reaction)` |  |

### [tests/test_engine.py](../tests/test_engine.py)

_Tests for ``TrainingEngine`` behaviors that don't require a full training run._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_warmup_disabled_keeps_base_lr()` | ``warmup_steps=0`` must not touch ``optimizer.param_groups[*]['lr']``. |
| func | `test_warmup_ramps_linearly_then_holds()` | Step k (1-indexed) gets lr = base * k/warmup; after warmup, lr is held at base. |
| func | `test_warmup_state_round_trips_through_checkpoint(tmp_path)` | ``global_step`` survives a save → load cycle so warmup doesn't restart on resume. |
| func | `test_warmup_legacy_checkpoint_defaults_to_zero(tmp_path)` | Checkpoints saved before warmup existed must still load (no KeyError). |

### [tests/test_featurization.py](../tests/test_featurization.py)

_Tests for graph featurization helpers._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_one_hot_encode_known_value()` |  |
| func | `test_one_hot_encode_unknown_value_uses_unknown_bin()` |  |
| func | `test_process_mapped_smiles_returns_correct_shapes()` |  |
| func | `test_process_mapped_smiles_invalid_raises()` |  |

### [tests/test_featurization_post_delta.py](../tests/test_featurization_post_delta.py)

_Featurization stability across bond-Δ steps._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_featurize_works_on_post_delta_mol()` | Output of apply_delta is sanitized and has atom maps → featurize must |
| func | `test_featurize_post_delta_matches_featurize_fresh_smiles()` | Featurizing the apply_delta output should give the same tensors as |
| func | `test_featurize_after_two_delta_steps()` | Multi-step prep: featurize after step 1 AND after step 2; both must |

### [tests/test_integration.py](../tests/test_integration.py)

_End-to-end smoke tests: dataset → collator → model → loss._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_dataset_to_loss_pipeline(sample_reactions, task_mode, num_classes, shift)` | Regression: this would have caught the per-atom vs per-pair spectator bug. |

### [tests/test_losses.py](../tests/test_losses.py)

_Tests for ``MaskedFocalLossWithSpectators``._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_loss_is_finite_scalar()` |  |
| func | `test_spectator_downweighting_reduces_loss()` |  |
| func | `test_class_weights_alter_loss()` |  |
| func | `test_all_padding_returns_zero_loss()` |  |
| func | `test_spectator_mask_accepts_per_atom_shape()` | Regression: collator emits per-atom (B, N) spectator masks; loss must |

### [tests/test_metrics.py](../tests/test_metrics.py)

_Tests for metric helpers._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_pr_auc_perfect_ranking_is_one()` |  |
| func | `test_pr_auc_inverted_ranking_is_low()` |  |
| func | `test_pr_auc_no_positives_returns_zero()` |  |
| func | `test_pr_auc_empty_returns_zero()` |  |
| func | `test_mechanism_metrics_no_reactions_present_3class()` | If all targets are no-change and model predicts no-change, there are no positives. |
| func | `test_mechanism_metrics_no_reactions_present_7class()` | Regression: with 7 classes the no-change index is 3, NOT 1. |
| func | `test_mechanism_metrics_detects_true_reaction_7class()` | 7-class: one off-center target, model predicts it correctly → exactly 1 tp. |
| func | `test_per_class_counters_and_pr_auc_7class()` | Per-class counters distinguish 'never predicted class c' from 'tried but wrong'. |
| func | `test_pr_auc_uses_full_non_no_change_mass_7class()` | Regression: PR-AUC scoring must use P(any non-no-change), not P(first)+P(last). |
| func | `test_n_rxn_preds_zero_when_model_collapsed()` | Class-collapsed model (always predicts no-change) reports 0 rare predictions. |
| func | `test_precision_recall_improve_with_one_correct_tp_7class()` | Sanity check: ONE correct rare-class TP → P>0 and R>0. |
| func | `test_precision_zero_when_model_tries_but_misses()` | Distinguish 'never tried' from 'tried but wrong': the latter has FP>0. |
| func | `test_pooled_pr_auc_differs_from_averaged_per_batch()` | Regression: epoch-level PR-AUC must pool scores across all batches, not average per-batch APs. |
| func | `test_pooled_pr_auc_per_class_matches_concatenated()` | Per-class pooled PR-AUC must equal AP on concatenated (probs[:,c], target==c). |
| func | `test_get_mechanism_metrics_exposes_pooled_buffers_for_epoch_aggregation()` | Engine relies on metrics returning detached CPU buffers under reserved keys. |
| func | `test_exact_match_top1_all_correct_is_one()` | All-correct argmax → EM@1 = 1.0 and hits == total. |
| func | `test_exact_match_top1_single_wrong_pair_is_zero_for_that_sample()` | One mispredicted pair in a sample disqualifies the whole sample from EM@1. |
| func | `test_exact_match_top1_mixed_batch_ratio()` | In a batch of B samples with K perfect predictions, EM@1 = K/B. |
| func | `test_exact_match_top1_skips_samples_with_no_valid_pairs()` | Samples masked down to <2 atoms contribute no upper-tri pairs and are excluded. |

### [tests/test_models.py](../tests/test_models.py)

_Tests for the ``DeltaMLP`` head._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_delta_mlp_output_shape_and_symmetry(num_classes)` |  |
| func | `test_delta_mlp_rejects_invalid_class_count()` |  |
| func | `test_delta_mlp_bias_init_matches_class_prior()` | RetinaNet bias-init: step-0 softmax should approximate ``class_prior``. |
| func | `test_delta_mlp_bias_init_validates_prior_length()` |  |
| func | `test_delta_mlp_with_edge_features()` | edge_dim>0 must accept (B, N, N, edge_dim) and produce same output shape. |
| func | `test_delta_mlp_edge_features_actually_used()` | Changing edge features must change the head's output. |
| func | `test_delta_mlp_edge_features_required_when_configured()` |  |
| func | `test_reaction_transformer_with_edge_features_in_head()` | Densified edge features must flow end-to-end without shape errors. |

### [tests/test_parquet_dataset.py](../tests/test_parquet_dataset.py)

_Smoke tests for ParquetMechDataset._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_stepwise_dataset_loads()` |  |
| func | `test_end_to_end_dataset_loads()` |  |
| func | `test_split_partitions_are_disjoint()` |  |
| func | `test_stepwise_steps_inherit_reaction_split()` | A reaction's steps must all land in the same split as the reaction. |

### [tests/test_parser.py](../tests/test_parser.py)

_Tests for ``MechUSPTOParser`` (CSV format)._

| Kind | Symbol | Summary |
|---|---|---|
| func | `csv_row_simple()` | Single-step reaction (synthetic) used to exercise parse_csv_row. |
| func | `test_parse_csv_row_returns_multi_step_reaction(csv_row_simple)` |  |
| func | `test_parse_csv_row_preserves_atom_maps(csv_row_simple)` |  |
| func | `test_parse_csv_row_records_metadata(csv_row_simple)` |  |
| func | `test_parse_csv_row_rejects_missing_arrow()` |  |
| func | `test_parse_csv_row_rejects_too_few_columns()` |  |
| func | `test_parse_csv_file_loads_all_rows(sample_csv_path)` | Parsing the real sample CSV yields one MultiStepReaction per row. |
| func | `test_parse_csv_file_assigns_deterministic_ids(sample_csv_path)` |  |
| func | `test_parse_csv_file_preserves_mechanistic_class(sample_csv_path)` |  |
| func | `test_parse_csv_file_real_data_has_atom_mapped_smiles(sample_csv_path)` |  |

### [tests/test_plot_history.py](../tests/test_plot_history.py)

_Tests for scripts/plot_history.py._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_load_history_round_trip(tmp_path)` |  |
| func | `test_load_history_rejects_missing_history_key(tmp_path)` |  |
| func | `test_load_history_rejects_empty(tmp_path)` |  |
| func | `test_plot_history_creates_png(tmp_path)` |  |
| func | `test_series_handles_missing_keys()` |  |

### [tests/test_resume.py](../tests/test_resume.py)

_Tests for resume-from-checkpoint behavior._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_load_state_restores_start_epoch_and_best(tmp_path)` |  |
| func | `test_load_state_rejects_incompatible_architecture(tmp_path)` |  |
| func | `test_load_state_warns_on_benign_change_and_loads(tmp_path, capsys)` |  |

### [tests/test_seeding_and_amp.py](../tests/test_seeding_and_amp.py)

_Regressions for #8 (deterministic seeding) and #12 (bf16 autocast)._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_seed_everything_makes_random_numpy_torch_reproducible()` | Two `seed_everything(42)` calls must yield identical RNG draws. |
| func | `test_seed_everything_sets_cudnn_deterministic_when_requested()` |  |
| func | `test_seed_everything_skips_cudnn_flags_when_deterministic_false()` |  |
| func | `test_seed_worker_reseeds_numpy_and_random_from_torch_initial_seed()` | Inside a worker, `_seed_worker` must derive numpy/random seeds from torch. |
| func | `test_config_use_amp_defaults_true()` |  |
| func | `test_engine_amp_disabled_on_cpu_even_when_use_amp_true()` | `use_amp=True` + CPU device must fall back to a no-op autocast context. |
| func | `test_engine_amp_enabled_on_cuda()` |  |
| func | `test_engine_amp_disabled_when_use_amp_false()` |  |

### [tests/test_tracking.py](../tests/test_tracking.py)

_Tests for the experiment-tracker abstraction._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_default_tracker_is_noop()` |  |
| func | `test_make_tracker_rejects_unknown_backend()` |  |
| func | `test_flatten_per_class_expands_lists()` |  |
| func | `test_wandb_tracker_raises_helpful_error_if_uninstalled()` | If wandb isn't installed, instantiating WandbTracker should fail clearly. |

### [tests/test_transformations.py](../tests/test_transformations.py)

_Regression tests for ``DeltaMatrixGenerator``._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_break_arrow_records_negative_delta()` | ``"1,2="`` breaks bond between atom-map 1 and 2. |
| func | `test_form_arrow_records_positive_delta()` | ``"=1,3"`` forms bond between atom-map 1 and 3. |
| func | `test_shift_arrow_records_break_and_form()` | ``"1,2=1,3"`` must record BOTH break(1,2)=-1 AND form(1,3)=+1. |
| func | `test_shift_arrow_does_not_write_phantom_leaving_incoming_pair()` | Explicit guard: the (leaving, incoming) cell must remain zero. |
| func | `test_shift_arrow_with_non_first_pivot_atom_is_skipped()` | Current parser only handles shifts where ``src_maps[0] == dst_maps[0]``. |
| func | `test_multiple_arrows_in_one_string_are_all_recorded()` | Semicolon-separated arrows compose into one delta matrix. |
| func | `test_empty_arrow_string_returns_zero_delta()` |  |

### [tests/test_transformations_diff.py](../tests/test_transformations_diff.py)

_Edge-case regression tests for ``DeltaMatrixGenerator.delta_from_reactants_products``._

| Kind | Symbol | Summary |
|---|---|---|
| func | `test_noop_delta_when_reactants_equal_products()` | R == P → Δ must be all zeros (sequential model's stop step). |
| func | `test_leaving_group_internal_bonds_are_masked()` | Bonds internal to atoms that exist only in R (a leaving group) must |
| func | `test_atom_only_in_products_does_not_crash_and_pads()` | P has an atom that R does not (an incoming reagent that wasn't in R). |
| func | `test_aromatic_bonds_yield_integer_deltas()` | Δ must be integer-valued on aromatic systems — kekulization should |
| func | `test_delta_is_symmetric()` | Δ[i,j] must equal Δ[j,i]; the generator symmetrizes via (Δ + Δ.T) / 2. |
| func | `test_delta_diagonal_is_zero()` | The diagonal carries no bond information (Δq lives in charge_delta). |
